export var CANVAS_WIDTH = 500;
export var CANVAS_HEIGHT = 600;
export var ORIGIN_X = 500 / 2;
export var ORIGIN_Y = 500 / 2;

export var INNER_RADIUS = 10;
export var OUTER_RADIUS = 240;

export var TUNNEL_SPEED_DIVISOR = 15;
export var TUNNEL_ACCEL_FACTOR = 0.015;
